{
  "name": "sales-board",
  "version": "1.0.0",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/postgres": "^0.9.0"
  }
}
